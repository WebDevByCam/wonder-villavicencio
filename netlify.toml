[build][build]

  publish = "dist"  publish = ".output/public"

  command = "npm run generate"  command = "bun install --frozen-lockfile && bun run generate"



[[redirects]][build.environment]

  from = "/*"  BUN_VERSION = "1.3.1"

  to = "/index.html"

  status = 200[context.production.environment]
  NODE_VERSION = "20"
  BUN_VERSION = "1.3.1"

[[redirects]]
  from = "/*"
  to = "/200.html"
  status = 200
